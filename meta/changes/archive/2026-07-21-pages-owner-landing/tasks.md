# Tasks

- [x] Swap pages: lab -> index.html (panel collapsed + tune toggle), engine demo -> engine.html, redirect stub at the old lab URL
- [x] Multi-page vite build (index, engine, stub) with base /hologlyph/
- [x] Retarget eval capture, CI health check + capture arg, demo-smoke, lab-shot
- [x] Verify the BUILT dist end to end (static server + Playwright): landing boots owner config (baseOpacity 0.075, iris #d78bf8, panel hidden), tune opens panel, stub redirects with ?tune, engine page serves
- [x] Gates: tsc, 228/228 vitest, lint, cairn hook, eval overall pass + negative control still failing correctly
