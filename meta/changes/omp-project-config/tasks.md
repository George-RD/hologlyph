# Tasks: omp-project-config

- [x] Write `.omp/config.yml` with `skills.ignoredSkills` globs for unrelated
      skill families
- [x] Verify globs against the loaded skill list (0 false matches) and confirm
      `omp config get skills.ignoredSkills` resolves from the repo
- [x] Land via feature branch PR, squash-merge
